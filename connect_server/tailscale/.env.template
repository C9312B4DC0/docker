# ----- Tailscale container config -----

# Image tag to use for Tailscale (e.g. latest, stable, v1.66.4, etc.)
CONTAINER_VERSION=latest

# Restart policy for the container (e.g. "unless-stopped", "always", "on-failure")
RESTART_POLICY=unless-stopped

# Timezone inside the container
TZ=Etc/UTC

# UID and GID that the container should use (common in linuxserver-style images)
# Adjust these to match your host user if needed.
PUID=1000
PGID=1000

# Tailscale auth key from the Tailscale admin console
TS_AUTHKEY=tskey-client-notAReal-OAuthClientSecret1Atawk

# Optional extra args: tags, routes, exit-node, etc.
# Example: --advertise-tags=tag:docker-node --advertise-exit-node
TS_EXTRA_ARGS=--advertise-tags=tag:docker-node

# Kernel networking vs userspace networking
# Typically false in privileged-style setups, true if you must avoid /dev/net/tun
TS_USERSPACE=false
